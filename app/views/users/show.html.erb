    
<h1>Users endpoint</h1>
<p>Here should be displayed the selected user information</p>
<section>
<%= link_to "Back", users_path(@users)%>
  <div class="user-card">
    <div class="user-img-div">
      <img src="<%= @user.photo %>" class='user-img'>
    </div>
    <div class="user-info">
      <h2>
        <%= @user.name %>
      </h2>
      <h3>
        Number of posts: <%= @user.posts_counter %>
      </h3>
    </div>
  </div>
  <div class="user-bio">
    <h3>
      Bio
    </h3>
    <p>
      <%= @user.bio %>
    </p>
  </div>
  <% @user.recent_posts.each do |post| %>
    <div class="user-post">
      <h3><%= post.title %></h3>
      <p><%= post.text %></p>
      <span>Comments: <%= post.CommentsCounter %></span>
      <span>Likes: <%= post.LikesCounter %></span>
    </div>
  <% end %>
  <%= link_to user_posts_path(@user) do %>
    <button type="button" class="see-all-posts-button">See all posts</button>
  <% end %>
  <section>
